window.onload = function () {
    if (window.jQuery) {
        // jQuery is loaded  
        console.log('loaded')
    } else {
        // jQuery is not loaded
        console.log('not loaded')

    }
}